import WorkspaceModal from './workspaceModal';

describe('test WorkspaceModal', () => {
  it('should get correct namespace', () => {
    expect(WorkspaceModal.namespace).toBe('workspaceModal');
  });
});
